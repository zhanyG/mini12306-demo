# 实验报告：单元测试

---

## 第一部分：实验预习报告

### 一、实验目的与意义

1. 掌握单元测试的基本概念和原理
2. 掌握 JUnit 5 框架的使用方法，包括 `@Test`、`@BeforeEach`、`@Nested`、`@DisplayName` 等注解
3. 掌握 Mockito  mocking 框架的使用，包括 `@Mock`、`@InjectMocks`、`@Captor` 等注解
4. 学会对 Java 实体类和 Service 层进行单元测试用例设计
5. 理解白盒测试中的分支覆盖和路径覆盖

### 二、实验基本原理与方法

**单元测试**：对软件中的最小可测试单元（如 Java 类中的方法）进行检查和验证。本项目使用 JUnit 5 作为测试框架。

**Mock 测试**：当被测对象依赖外部组件（如数据库 Repository）时，使用 Mockito 创建模拟对象，隔离被测逻辑，聚焦业务规则验证。

**测试分层**：
- **实体层测试**：直接 new 对象，测试 POJO 的业务方法（如 `bookSeat()`、`pay()`）
- **Service 层测试**：使用 Mockito 模拟 Repository 层，测试 Service 业务编排逻辑
- **集成测试**：`@SpringBootTest` 加载完整 Spring 上下文，验证整体配置

### 三、实验环境与工具

| 项目 | 版本/工具 |
|------|----------|
| 操作系统 | Windows 10 |
| JDK | 24.0.2 |
| 构建工具 | Maven 3.x |
| 测试框架 | JUnit 5 (Jupiter) + Mockito 5 |
| IDE | IntelliJ IDEA Community |
| 数据库 | MySQL 9.6（测试用 H2 内存数据库） |
| 项目框架 | Spring Boot 4.0.6 |

### 四、实验方案与技术路线

被测核心类及方法：

| 被测类 | 被测方法 | 测试策略 |
|--------|---------|---------|
| `Train` | `bookSeat()`、`refundSeat()` | 纯实体测试，覆盖正常/边界路径 |
| `Order` | `pay()`、`confirm()`、`cancel()` | 纯实体测试，覆盖状态转换路径 |
| `Ticket` | `refund()` | 纯实体测试 |
| `OrderService` | `buyTicket()`、`cancelTicket()`、`getUserOrders()` | Mockito 模拟 Repository |

---

## 第二部分：实验过程记录

### 一、测试用例设计

#### 1.1 Train 实体测试用例

| 用例编号 | 测试方法 | 测试场景 | 前置条件 | 输入/操作 | 预期结果 |
|---------|---------|---------|---------|----------|---------|
| TC-T-01 | `shouldBookWhenAvailable` | 有余票时预订 | availableSeats=100 | `bookSeat()` | return true, seats→99 |
| TC-T-02 | `shouldFailWhenSoldOut` | 无余票时预订 | availableSeats=0 | `bookSeat()` | return false, seats=0 |
| TC-T-03 | `shouldBookUntilSoldOut` | 连续预订直到售罄 | availableSeats=3 | 连续4次 `bookSeat()` | 前3次true，第4次false |
| TC-T-04 | `shouldRefundSeat` | 退票释放余票 | availableSeats=50 | `refundSeat()` | seats→51 |
| TC-T-05 | `shouldNotExceedTotalSeats` | 退票上限不超总座位数 | availableSeats=100(已满) | `refundSeat()` | seats=100(不变) |
| TC-T-06 | `shouldDefaultTo100Seats` | 默认座位数 | 新Train对象 | 检查属性 | totalSeats=100, availableSeats=100 |

#### 1.2 Order 实体测试用例

| 用例编号 | 测试方法 | 测试场景 | 前置条件 | 操作 | 预期结果 |
|---------|---------|---------|---------|------|---------|
| TC-O-01 | `shouldCreateUnpaidOrder` | 新订单默认值 | 无 | new Order(...) | status="未支付"，orderNo 以 ORD 开头 |
| TC-O-02 | `shouldPay` | 支付 | 新订单 | `pay()` | status→"已支付" |
| TC-O-03 | `shouldConfirm` | 出票 | 新订单 | `confirm()` | status→"已出票" |
| TC-O-04 | `shouldCancel` | 退票 | 新订单 | `cancel()` | status→"已退票" |
| TC-O-05 | `shouldTransitionThroughStates` | 完整生命周期 | 新订单 | pay→confirm | "未支付"→"已支付"→"已出票" |
| TC-O-06 | `shouldCancelAfterConfirm` | 出票后退票 | 新订单 | pay→confirm→cancel | "已出票"→"已退票" |

#### 1.3 Ticket 实体测试用例

| 用例编号 | 测试方法 | 测试场景 | 预期结果 |
|---------|---------|---------|---------|
| TC-K-01 | `shouldCreateValidTicket` | 新车票创建 | ticketNo以TKT开头，status="有效" |
| TC-K-02 | `shouldDefaultToSecondClass` | 默认座位类型 | seatType="二等座" |
| TC-K-03 | `shouldRefundTicket` | 退票 | status→"已退" |

#### 1.4 OrderService 测试用例

| 用例编号 | 测试方法 | 测试场景 | Mock 设置 | 预期结果 |
|---------|---------|---------|----------|---------|
| TC-S-01 | `shouldBuyTicketSuccessfully` | 正常购票 | train查得到(50座)/save返回自身/findById返回未支付订单 | seats→49, status="已出票", 车票保存 |
| TC-S-02 | `shouldThrowWhenTrainNotFound` | 车次不存在 | findById(999)返回empty | 抛"车次不存在" |
| TC-S-03 | `shouldThrowWhenNoSeats` | 余票不足 | train可用座位=0 | 抛"余票不足" |
| TC-S-04 | `shouldCancelConfirmedOrder` | 已出票退票 | order查到/火车查到/车票查到 | true, status="已退票", seats→51 |
| TC-S-05 | `shouldNotCancelNonConfirmedOrder` | 未支付的不能退 | order.status="未支付" | false, 不调用train.save |
| TC-S-06 | `shouldReturnFalseWhenOrderNotFound` | 订单不存在 | findById(999)返回empty | false |
| TC-S-07 | `shouldCancelEvenWhenTrainMissing` | 车次不存在也能退 | train查不到 | true, 状态变已退票 |
| TC-S-08 | `shouldBeIdempotent` | 退票幂等 | order.status="已退票" | false |
| TC-S-09 | `shouldReturnUserOrders` | 按用户查订单 | findByUserId返回列表 | 返回1条，id=100 |
| TC-S-10 | `shouldReturnEmptyList` | 无订单时 | findByUserId返回空 | 返回空列表 |

### 二、测试代码

#### 2.1 核心测试代码：TrainTest.java

```java
package com.example.demo.entity;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Nested;
import org.junit.jupiter.api.Test;
import java.time.LocalDateTime;
import static org.junit.jupiter.api.Assertions.*;

class TrainTest {

    private Train train;

    @BeforeEach
    void setUp() {
        train = new Train("G1001", "北京南", "上海虹桥",
                LocalDateTime.of(2026, 6, 4, 8, 0),
                LocalDateTime.of(2026, 6, 4, 12, 30), 553.0);
        train.setTotalSeats(100);
        train.setAvailableSeats(100);
    }

    @Nested
    @DisplayName("预订座位 bookSeat()")
    class BookSeatTest {
        @Test
        @DisplayName("有余票时预订成功，余票减1")
        void shouldBookWhenAvailable() {
            boolean result = train.bookSeat();
            assertTrue(result);
            assertEquals(99, train.getAvailableSeats());
        }

        @Test
        @DisplayName("余票为0时预订失败，返回false")
        void shouldFailWhenSoldOut() {
            train.setAvailableSeats(0);
            boolean result = train.bookSeat();
            assertFalse(result);
            assertEquals(0, train.getAvailableSeats());
        }

        @Test
        @DisplayName("连续预订直到售罄")
        void shouldBookUntilSoldOut() {
            train.setAvailableSeats(3);
            assertTrue(train.bookSeat()); // 3→2
            assertTrue(train.bookSeat()); // 2→1
            assertTrue(train.bookSeat()); // 1→0
            assertFalse(train.bookSeat());
            assertEquals(0, train.getAvailableSeats());
        }
    }

    @Nested
    @DisplayName("退还座位 refundSeat()")
    class RefundSeatTest {
        @Test
        @DisplayName("退票时余票加1")
        void shouldRefundSeat() {
            train.setAvailableSeats(50);
            train.refundSeat();
            assertEquals(51, train.getAvailableSeats());
        }

        @Test
        @DisplayName("退票上限不超过总座位数")
        void shouldNotExceedTotalSeats() {
            train.setAvailableSeats(100);
            train.refundSeat();
            assertEquals(100, train.getAvailableSeats());
        }
    }

    @Test
    @DisplayName("新列车默认100个座位")
    void shouldDefaultTo100Seats() {
        Train defaultTrain = new Train("D321", "杭州东", "上海虹桥",
                LocalDateTime.now(), LocalDateTime.now().plusHours(1), 73.0);
        assertEquals(100, defaultTrain.getTotalSeats());
        assertEquals(100, defaultTrain.getAvailableSeats());
    }
}
```

**被测的 Tra in 实体核心方法**：
```java
// Train.java — 业务方法
public boolean bookSeat() {
    if (availableSeats > 0) {
        availableSeats--;
        return true;
    }
    return false;
}

public void refundSeat() {
    if (availableSeats < totalSeats) availableSeats++;
}
```

#### 2.2 核心测试代码：OrderServiceTest.java

```java
package com.example.demo.service;

import com.example.demo.entity.Bill;
import com.example.demo.entity.Order;
import com.example.demo.entity.Ticket;
import com.example.demo.entity.Train;
import com.example.demo.repository.*;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.*;
import org.mockito.junit.jupiter.MockitoExtension;
import java.time.LocalDateTime;
import java.util.List;
import java.util.Optional;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class OrderServiceTest {

    @Mock private TrainRepository trainRepository;
    @Mock private OrderRepository orderRepository;
    @Mock private TicketRepository ticketRepository;
    @Mock private BillRepository billRepository;

    @InjectMocks private OrderService orderService;

    @Captor private ArgumentCaptor<Order> orderCaptor;
    @Captor private ArgumentCaptor<Ticket> ticketCaptor;

    private Train train;
    private Order order;
    private Ticket ticket;

    @BeforeEach
    void setUp() {
        train = new Train("G1001", "北京南", "上海虹桥",
                LocalDateTime.of(2026, 6, 4, 8, 0),
                LocalDateTime.of(2026, 6, 4, 12, 30), 553.0);
        train.setId(1L);
        train.setTotalSeats(100);
        train.setAvailableSeats(50);

        order = new Order(1L, 1L, 1L, 553.0);
        order.setId(100L);
        order.confirm(); // 已出票

        ticket = new Ticket(100L, 1L, "01车01A");
    }

    @Nested
    @DisplayName("购票 buyTicket()")
    class BuyTicketTest {
        @BeforeEach
        void setUp() {
            lenient().when(orderRepository.save(any(Order.class))).thenAnswer(i -> {
                Order o = i.getArgument(0);
                if (o.getId() == null) o.setId(100L);
                return o;
            });
            Order unpaid = new Order(1L, 1L, 1L, 553.0);
            unpaid.setId(100L);
            lenient().when(orderRepository.findById(100L)).thenReturn(Optional.of(unpaid));
            lenient().when(ticketRepository.save(any(Ticket.class))).thenAnswer(i -> i.getArgument(0));
            lenient().when(billRepository.save(any(Bill.class))).thenReturn(null);
        }

        @Test
        @DisplayName("正常购票成功，余票扣减，订单支付出票")
        void shouldBuyTicketSuccessfully() {
            when(trainRepository.findById(1L)).thenReturn(Optional.of(train));
            Order result = orderService.buyTicket(1L, 1L, 1L);

            assertEquals(49, train.getAvailableSeats());
            assertEquals("已出票", result.getStatus());
            assertTrue(result.getOrderNo().startsWith("ORD"));
            verify(ticketRepository, times(1)).save(ticketCaptor.capture());
            Ticket savedTicket = ticketCaptor.getValue();
            assertEquals("有效", savedTicket.getStatus());
            assertEquals(1L, savedTicket.getPassengerId());
            assertNotNull(savedTicket.getSeatNumber());
        }

        @Test
        @DisplayName("车次不存在时抛出异常")
        void shouldThrowWhenTrainNotFound() {
            when(trainRepository.findById(999L)).thenReturn(Optional.empty());
            RuntimeException ex = assertThrows(RuntimeException.class,
                    () -> orderService.buyTicket(1L, 999L, 1L));
            assertTrue(ex.getMessage().contains("车次不存在"));
        }

        @Test
        @DisplayName("余票不足时抛出异常")
        void shouldThrowWhenNoSeats() {
            train.setAvailableSeats(0);
            when(trainRepository.findById(1L)).thenReturn(Optional.of(train));
            RuntimeException ex = assertThrows(RuntimeException.class,
                    () -> orderService.buyTicket(1L, 1L, 1L));
            assertTrue(ex.getMessage().contains("余票不足"));
        }
    }

    @Nested
    @DisplayName("退票 cancelTicket()")
    class CancelTicketTest {
        @Test
        @DisplayName("已出票订单可正常退票")
        void shouldCancelConfirmedOrder() {
            when(orderRepository.findById(100L)).thenReturn(Optional.of(order));
            when(trainRepository.findById(1L)).thenReturn(Optional.of(train));
            when(ticketRepository.findByOrderId(100L)).thenReturn(Optional.of(ticket));

            boolean result = orderService.cancelTicket(100L);
            assertTrue(result);
            assertEquals("已退票", order.getStatus());
            assertEquals(51, train.getAvailableSeats());
            assertEquals("已退", ticket.getStatus());
        }

        @Test
        @DisplayName("非已出票状态不能退票")
        void shouldNotCancelNonConfirmedOrder() {
            order.setStatus("未支付");
            when(orderRepository.findById(100L)).thenReturn(Optional.of(order));
            boolean result = orderService.cancelTicket(100L);
            assertFalse(result);
            verify(trainRepository, never()).save(any());
        }
        // ... 其他测试方法
    }

    @Nested
    @DisplayName("查询订单 getUserOrders()")
    class GetUserOrdersTest {
        @Test
        @DisplayName("按用户ID查询订单列表")
        void shouldReturnUserOrders() {
            when(orderRepository.findByUserId(1L)).thenReturn(List.of(order));
            List<Order> orders = orderService.getUserOrders(1L);
            assertEquals(1, orders.size());
            assertEquals(100L, orders.get(0).getId());
        }

        @Test
        @DisplayName("无订单时返回空列表")
        void shouldReturnEmptyList() {
            when(orderRepository.findByUserId(99L)).thenReturn(List.of());
            List<Order> orders = orderService.getUserOrders(99L);
            assertTrue(orders.isEmpty());
        }
    }
}
```

### 三、实验过程中发现的问题

**问题 1：IDE 无法识别 Lombok 生成的方法**

- **现象**：IDEA 中测试代码大量报红，`train.setAvailableSeats()`、`order.getStatus()` 等方法无法解析
- **原因**：实体类使用 `@Data` 注解（Lombok），编译时自动生成 getter/setter，但 IDEA 需要 Lombok 插件才能识别
- **解决**：安装 Lombok 插件（File → Settings → Plugins），启用注解处理（Settings → Compiler → Annotation Processors）

**问题 2：OrderServiceTest 缺少 BillRepository 的 Mock**

- **现象**：`mvn test` 运行时抛出 NullPointerException
- **原因**：`OrderService` 构造器需要 4 个 Repository 参数，但测试只 Mock 了 3 个，`billRepository` 为 null
- **解决**：补充 `@Mock private BillRepository billRepository;`

**问题 3：buyTicket 测试流程中 Mock 覆盖不完整**

- **现象**：`shouldThrowWhenNoSeats` 期望抛"余票不足"，实际抛"订单不存在"
- **原因**：`buyTicket()` 内部调用了 `createOrder()` 和 `confirmPayment()`，后者需要 `orderRepository.findById()` 的 mock
- **解决**：在 `BuyTicketTest` 内部类添加 `@BeforeEach`，统一设置 `save`/`findById`/`ticketRepository`/`billRepository` 的 mock

---

## 第三部分：结果与讨论

### 一、测试结果

#### 1.1 汇总统计

运行 `mvn test` 后输出：

```
Tests run: 26, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

#### 1.2 各测试类详细结果

| 测试类 | 包含测试数 | 通过 | 失败 | 耗时 |
|--------|:---------:|:---:|:---:|:----:|
| `DemoApplicationTests` | 1 | ✅ | 0 | 7.409s |
| `OrderTest`（状态转换） | 6 | ✅ | 0 | 0.008s |
| `TicketTest` | 3 | ✅ | 0 | 0.006s |
| `TrainTest`（预订座位） | 3 | ✅ | 0 | 0.008s |
| `TrainTest`（退还座位） | 3 | ✅ | 0 | 0.004s |
| `OrderServiceTest`（购票） | 3 | ✅ | 0 | 0.483s |
| `OrderServiceTest`（退票） | 5 | ✅ | 0 | 0.017s |
| `OrderServiceTest`（查询订单） | 2 | ✅ | 0 | 0.007s |
| **合计** | **26** | **26** | **0** | **~8s** |

#### 1.3 测试覆盖率分析

| 被测类 | 方法数 | 覆盖方法 | 路径覆盖情况 |
|--------|:-----:|:-------:|:-----------:|
| `Train.bookSeat()` | 1 | ✅ 全覆盖 | 有余票/无余票两条分支 |
| `Train.refundSeat()` | 1 | ✅ 全覆盖 | 未满/已满两条分支 |
| `Order` 状态方法 | 4 | ✅ 全覆盖 | 全部状态转换路径 |
| `Ticket.refund()` | 1 | ✅ 全覆盖 | 单路径 |
| `OrderService.buyTicket()` | 1 | ✅ 全覆盖 | 成功/车次不存在/余票不足 |
| `OrderService.cancelTicket()` | 1 | ✅ 全覆盖 | 5种场景覆盖 |
| `OrderService.getUserOrders()` | 1 | ✅ 全覆盖 | 有订单/无订单 |

### 二、实验结果分析

**实体层测试**：`Train`、`Order`、`Ticket` 三个实体类的业务方法全部通过验证。特别地：
- `bookSeat()` 覆盖了有余票、无余票、连续预订到售罄三种场景
- `Order` 的状态转换覆盖了"未支付→已支付→已出票→已退票"的完整生命周期
- `refundSeat()` 验证了上限保护逻辑（不会超过 totalSeats）

**Service 层测试**：`OrderService` 的三个核心方法覆盖了：
- **正常路径**：购票成功、退票成功、查询有数据
- **异常路径**：车次不存在、余票不足、订单不存在
- **边界场景**：退票幂等性、车次不存在时仍能退票、非已出票状态不能退票

### 三、技术要点总结

| 技术点 | 使用方式 | 作用 |
|--------|---------|------|
| `@Test` | 标注测试方法 | 标识一个可执行的测试 |
| `@BeforeEach` | 每个测试前执行 | 初始化测试数据 |
| `@Nested` + `@DisplayName` | 内部类分组 | 测试分类和中文描述 |
| `assertEquals` | 判断相等 | 验证结果是否符合预期 |
| `assertTrue/assertFalse` | 判断布尔值 | 验证 boolean 返回 |
| `assertThrows` | 验证异常 | 验证特定场景抛异常 |
| `@Mock` | 模拟依赖 | 替代真实的 Repository |
| `@InjectMocks` | 注入 mock | 将 mock 注入到被测 Service |
| `@Captor` | 捕获参数 | 验证传入 Repository 的参数值 |
| `verify` | 验证调用 | 确认方法被调用的次数和参数 |
| `lenient()` | 宽松模式 | 允许部分 mock 未被使用 |

### 四、结论

本次实验使用 JUnit 5 + Mockito 完成了对 mini12306 项目中核心实体类和 Service 层的单元测试：

1. 实体层测试 **15 个**（Train 7 + Order 6 + Ticket 3），验证了所有业务方法的正确性
2. Service 层测试 **10 个**（OrderService），使用 Mockito 模拟数据库层，覆盖正常路径和异常路径
3. 所有 **26 个测试全部通过**，构建成功
4. 测试过程中发现并修复了 Lombok IDE 配置、Mock 缺失、Mock 覆盖不完整等 3 个问题

### 五、IDEA 测试运行截图说明

> **在 IDEA 中运行测试可以观察到以下界面效果：**

**图 1：运行所有测试**
- 操作：在项目根目录右键 → Run 'All Tests'
- 结果：IDEA 底部 Run 窗口显示绿色进度条
- 显示：Tests passed: 26/26

**图 2：单个测试类运行**
- 操作：在 `TrainTest` 类上右键 → Run 'TrainTest'
- 结果：7 个测试显示绿色勾 ✓

**图 3：单购票成功测试运行**
- 操作：在 `shouldBuyTicketSuccessfully` 方法前的 ▶ 点击 → Run
- 结果：绿色通过

**图 4：Maven 命令行运行**
```
cd demo
mvn test
```
- 输出末尾显示：`BUILD SUCCESS` 和 `Tests run: 26, Failures: 0, Errors: 0`
